import { UniversidadesPaisPipe } from './universidades-pais.pipe';

describe('UniversidadesPaisPipe', () => {
  it('create an instance', () => {
    const pipe = new UniversidadesPaisPipe();
    expect(pipe).toBeTruthy();
  });
});
